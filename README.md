# Faveradio

> 把收藏、播放、歌词与实时视觉放进同一个 Windows 音乐空间。

![Faveradio 沉浸式播放界面](./docs/assets/readme/cinema-beat-smoke.png)

Faveradio 是一款面向 Windows 的桌面音乐播放器。它既可以播放本地音乐，也可以连接用户自己的音乐平台账号，并在播放过程中提供歌词舞台、3D 歌单架、音频响应视觉、摄像头手势、Smart Favorites 和 AI 选曲。

它不是简单的“播放器加频谱”：歌曲、歌词、封面、节奏、收藏标签和视觉参数被组织在同一个交互系统里。你可以只把它当作本地播放器，也可以逐步开启平台歌单、AI 推荐、Wallpaper Engine 或 EmoMusic 等高级能力。

> 项目对外名称现为 **Faveradio**。为兼容既有用户数据、更新链路和安装安全逻辑，当前 `2.3.0` 安装包文件名、进程名及部分内部字段仍沿用 `Mineradio`；这不会影响正常使用。

## 主要能力

### 音乐与收藏

- 导入单曲或整个本地音乐文件夹，支持本地歌词与封面。
- 搜索和浏览网易云音乐、QQ 音乐、酷狗音乐、汽水音乐等来源。
- 使用用户自己的平台登录状态读取歌单、收藏及可用音源。
- 将多个平台歌单合并、去重并保存为普通的“合并收藏夹”。
- 支持顺序、循环、随机和 AI 播放，并保留真实的上一首播放历史。
- 提供每日推荐、继续播放、本地曲库和最近播放入口。

### Smart Favorites 与 AI 播放

- 为当前歌单分析风格、语言、情绪、年代和自定义语义标签。
- 标签支持“不选 / 必须符合 / 可以符合”三种状态。
- 可把歌曲封面或歌名拖到标签上，保存人工标注。
- AI 只在当前播放歌单中选曲，不会静默扩大推荐范围。
- LLM 不可用时仍可使用人工标注、平台标签和已有缓存，普通播放不受影响。
- API Key 由 Electron 主进程通过 `safeStorage` 保存，不写入源码、渲染器或 `localStorage`。

### 沉浸式视觉

- 歌词舞台：主歌词、翻译、发光、景深、滚动和镜头联动。
- 3D 歌单架：在沉浸界面中浏览歌单和队列。
- 多套内置视觉预设，包括 Emily、星河、唱片、星球、滚筒、虚空、月蚀圣环、雨幕霓虹、折光蝶群和深海绽放。
- CyberRibbon：保留原始 Three.js 丝带、地形、星辰和后处理逻辑。
- SpaceVenom：保留原始 WebGL 2 磁流体、Starfield 和独立分频响应。
- EmoMusic：将 Face Mesh、视线、音乐 RMS、情绪向量、粒子、涟漪、射线和星河组合成实时视觉。
- 用户视觉存档支持保存、加载、JSON 导入导出和 MR2 短码分享。

### Windows 桌面能力

- 无边框桌面窗口、托盘和单实例运行。
- 完整桌面模式，将 Faveradio 场景放入 Windows 桌面层。
- 本地 Wallpaper Engine 项目识别、索引、静音运行和视觉参数控制。
- 自定义缓存目录、后台性能策略和低配置画质档位。

完整桌面模式、桌面图标层和 Wallpaper Engine Scene 运行属于 Windows 专用实验能力。首次使用前建议先保存工作，并确认显卡驱动和 Wallpaper Engine 状态正常。

## 系统要求

- Windows 10 或 Windows 11，64 位。
- 建议使用支持 WebGL 2 的独立显卡或较新的核显。
- 本地音乐播放不需要平台账号或 LLM。
- 摄像头手势、Face Mesh 和 EmoMusic 需要可用摄像头。
- Wallpaper Engine 联动需要本机已安装相应软件及项目文件。
- AI 标签与云端 VLM 是可选功能，需要用户自行提供兼容服务和 API Key。

## 下载与安装

当前正式版本：`2.3.0`

- [打开 Faveradio 2.3.0 Release](https://github.com/WhatTheLuck/Mineradio/releases/tag/v2.3.0)
- Windows 安装包：`Mineradio-2.3.0-Setup.exe`
- SHA-256：`05b22ad12e1ff381f127aa43738c4fbfc67cb017be024ba5a570d473a6453fe2`

安装步骤：

1. 在 Release 页面下载 `Mineradio-2.3.0-Setup.exe`。
2. 可选：在 PowerShell 中核对安装包哈希：

   ```powershell
   Get-FileHash .\Mineradio-2.3.0-Setup.exe -Algorithm SHA256
   ```

3. 运行安装包，按提示选择安装位置。
4. 安装器会使用独立的 `Mineradio` 目录，这是当前版本保留的兼容目录名。
5. 安装完成后从桌面快捷方式或开始菜单启动。

当前安装包未使用商业代码签名证书。浏览器或 Windows SmartScreen 可能提示“未知发布者”；请确认文件来自本仓库 Release，并核对上述 SHA-256。若安全软件明确报告恶意内容或文件哈希不一致，请停止运行并重新下载。

## 第一次使用

### 1. 进入播放器

启动动画结束后，点击画面或按 `Enter` 进入 Home。没有登录任何音乐平台也可以直接导入本地音乐。

### 2. 导入本地音乐

点击顶部的导入按钮，可选择：

- **单曲文件**：导入并立即播放。
- **多曲文件夹**：批量加入本地曲库和播放队列。
- **封面图片**：替换当前歌曲的显示封面。

本地曲库会保留可恢复的文件记录。FLAC 标签、内嵌封面、同步歌词和同名 `.lrc` 会在可用时读取。若移动或删除源音乐文件，需要重新导入对应位置。

### 3. 登录音乐平台

打开账号区域，选择需要使用的平台并按页面提示登录。Faveradio 只使用用户自己的账号状态，不提供绕过会员、付费或地区限制的能力。

不同平台的搜索、歌单、试听和完整播放权限可能不同。出现“能看到歌单但不能播放”时，优先检查当前平台的登录状态、会员权限和歌曲本身的可用性。

### 4. 开始播放

可从搜索、Home、平台歌单、本地曲库、合并收藏夹或 3D 歌单架启动播放。底部控制栏提供播放、上一首、下一首、播放模式、音量和 AI 模式。

播放器会根据当前歌曲加载歌词、封面和节奏信息。某项视觉分析失败不会中断基础音频播放。

## 合并收藏夹教程

合并收藏夹用于把多个来源的歌单整理为一个本地普通歌单。

1. 在 Home 打开 **合并收藏夹**。
2. 勾选需要合并的平台歌单或已有收藏夹。
3. 输入新收藏夹名称。
4. 点击 **建立合并收藏夹**。
5. 等待全部来源读取完成并去重。
6. 创建成功后可直接播放，也可以像普通收藏夹一样再次使用。

如果任一来源读取失败，界面会显示错误并允许重试，不会把缺少歌曲的不完整结果伪装为成功收藏夹。

## AI 播放教程

### 开启 AI 模式

1. 先播放任意歌单。
2. 点击底部播放栏的 **AI** 按钮。
3. 在播放栏上方输入当前想听的关键词，例如“夜晚”“女声”“器乐”“节奏强”。
4. 按 `Enter` 将关键词保存为标签。
5. 点击标签，在三种状态间循环：

   - **不选**：不影响推荐。
   - **必须符合**：硬性筛选条件。
   - **可以符合**：提高推荐权重，但不是硬性要求。

AI 下一首只会从当前歌单中选择。开启多个“必须符合”条件后，如果没有歌曲满足条件，系统会如实提示，不会悄悄放宽要求。

### 人工标注

把当前歌曲的封面或歌名拖到某个标签上，可以保存人工标注。人工标注与平台元数据、LLM 分析结果共同参与选曲。

### 配置 LLM

打开 **合并收藏夹 → LLM 设置**，填写：

- Base URL：OpenAI-compatible `/v1` 服务地址。
- Model：服务端提供的模型名称。
- API Key：用户自己的密钥。

建议先点击 **单独测试响应**，成功后再点 **安全保存**。大量歌曲分析会产生 API 调用和可能的服务费用，请先确认服务商价格与额度。

也可以通过环境变量提供配置，环境变量优先级高于界面保存值：

```powershell
$env:MINERADIO_LLM_BASE_URL = "https://your-provider.example/v1"
$env:MINERADIO_LLM_MODEL = "your-model"
$env:MINERADIO_LLM_API_KEY = "your-api-key"
npm start
```

不要把真实 API Key 写进代码、提交到 Git，或粘贴到公开 Issue 和日志中。

更多细节见 [合并收藏夹与 AI 播放](./docs/SMART_FAVORITES.md)。

## 视觉系统教程

### 打开视觉控制台

播放歌曲后打开视觉控制台。控制台按任务分为：

- **常用**：视觉预设和最常用参数。
- **界面**：背景、玻璃、颜色和窗口表现。
- **歌词**：字体、布局、清晰度、翻译和发光。
- **动效**：粒子、镜头、CyberRibbon、SpaceVenom 与 EmoMusic。
- **歌单架**：3D 歌单架的显示、镜头和内容。
- **系统**：性能、缓存、摄像头和桌面模式。

建议先选择整体预设，再调整细节。低配置设备可把画质切到“低”或“中”，关闭高质量歌词纹理、AI 景深和不需要的背景层。

### 保存视觉参数

在用户存档区域可以：

- 保存当前视觉状态。
- 重命名和加载存档。
- 导出或导入 JSON。
- 复制和粘贴 MR2 短码。

视觉存档保存的是参数，不包含音乐文件、账号凭据或 API Key。

### CyberRibbon 与 SpaceVenom

进入 **控制台 → 动效**：

- **赛博丝带**区域可选择原始预设、编辑参数、保存和加载。
- **星际磁流体**区域可选择 Venom/Blood/Bubble/Water 等预设并调整星空和音频响应。

两套视觉在独立 iframe 中运行，退出预设后会销毁对应渲染循环。自定义参数默认保存在程序目录下：

```text
visual-presets/cyber/*.json
visual-presets/space/*.json
```

如果安装目录不可写，保存会显示真实错误，不会静默保存到未知位置。更多说明见 [原项目视觉与参数保存](./docs/SOURCE_VISUALS.md)。

## EmoMusic 与摄像头

### 基础 Face Mesh

1. 在视觉预设中选择 **EmoMusic**。
2. 允许应用使用摄像头。
3. 选择粒子、网格或平面等面部显示模式。
4. 根据设备性能选择轻量、均衡或精细摄像头档位。
5. 正对摄像头并保持脸部光线均匀。

Face Mesh 和视线估算在本地处理。仅使用本地 Face Mesh 时，不会上传摄像头画面。检测不到人脸时，Faveradio 会停止情绪生成、丢弃进行中的结果并清除过期状态。

### 视线校准

1. 打开 **瞳孔与视线 · 轻量**。
2. 保持“识别瞳孔中心与视线”开启。
3. 正视屏幕中央。
4. 点击 **注视中央并校准**。
5. 按需开启双眼瞳孔标记和 RMS 射线。

普通 RGB 摄像头只能提供交互级视线估算，结果会受眼镜反光、光照、眨眼、头部角度和摄像头位置影响，不能替代专业眼动仪。

详见 [EmoMusic 瞳孔与视线识别](./docs/EMOMUSIC_EYE_TRACKING.md)。

### 可选云端 VLM

如果用户主动配置并开启云端 VLM 情绪分析，应用会通过 Electron 主进程把压缩后的当前画面或面部区域发送到用户配置的模型服务。API Key 不会进入渲染器，但图像数据会离开本机。使用前请阅读模型服务商的隐私政策，不需要该能力时保持关闭。

## 手势控制

在 **控制台 → 系统 → 摄像头交互** 中开启播放手势。当前界面提示的主要动作包括：

- 张掌：推开视觉元素。
- 捏合：旋转。
- 握拳：收束。
- 左右滑动：切歌。
- V 手势：播放或暂停。
- 食指：调节音量。
- 拇指：喜欢当前歌曲。
- 三指：控制歌词。

手势需要稳定保持后才执行，以降低误触。摄像头显示分辨率和模型推理质量是独立设置；提高画面分辨率不等于提高识别质量。

## Wallpaper Engine 与完整桌面模式

### 导入 Wallpaper Engine 项目

1. 打开 **控制台 → 界面 → Wallpaper Engine**。
2. 点击 **识别 / 导入**。
3. 选择自动识别本机库、导入目录，或导入单个项目/场景包。
4. 选择项目后调整透明度、位置和缩放。
5. 点击 **恢复原背景** 可退出当前壁纸。

Faveradio 只为项目建立本地索引，不会复制完整创意工坊目录。Video 项目由播放器直接播放；可用的 Scene 项目通过本机 Wallpaper Engine 运行。Scene 缓存只处理副本，原始项目不被修改。

### 完整桌面模式

完整桌面模式会把整个 Faveradio 界面放入 Windows 桌面层。进入后可通过控制台或配置的热键切换交互层和退出。若桌面图标、鼠标输入或窗口层级异常，先退出该模式，再从托盘完全关闭并重新启动应用。

## 数据、缓存与隐私

Faveradio 的用户数据可能包括：

- 平台登录状态。
- 本地曲库索引、搜索历史和播放历史。
- 自定义封面、歌词和节奏分析缓存。
- 合并收藏夹、歌曲标签和 AI 分析缓存。
- 视觉存档和外部视觉参数。
- 经系统安全存储加密的 LLM/VLM 凭据。

这些数据应只保存在本机用户数据目录或用户选择的缓存目录中，不应提交到仓库。

Smart Favorites 的普通状态和缓存保存在兼容文件 `smart-favorites.json`；加密凭据保存在 `smart-favorites-llm.json`。删除用户数据前请先备份需要保留的歌单和视觉存档。

更多说明见 [隐私说明](./PRIVACY.md) 和 [第三方端口与依赖](./docs/THIRD_PARTY_PORTS.md)。

## 性能建议

如果运行卡顿，可依次尝试：

1. 将画质档位改为“低”或“中”。
2. 使用默认 VSync，避免设置高于显示器刷新率的固定帧率。
3. 将歌词纹理清晰度降为 `1×` 或 `2×`。
4. 关闭高质量上下文歌词、AI 景深和不需要的背景层。
5. EmoMusic 摄像头使用“轻量”档位。
6. 暂停 Wallpaper Engine Scene、CyberRibbon 或 SpaceVenom 等高负载视觉。
7. 在本地缓存面板检查歌词、节奏图和壁纸缓存占用。

低配置优化原则见 [低配置优化说明](./docs/LOW_SPEC_OPTIMIZATION_DOCTRINE.md)。

## 常见问题

### 点击歌曲后没有声音

- 检查系统输出设备和 Faveradio 音量。
- 平台歌曲请确认账号仍处于登录状态，并具有对应歌曲的播放权限。
- 本地歌曲请确认原文件没有被移动或删除。
- 尝试下一首，确认问题是单曲音源还是全局音频输出。

### 平台歌单能读取，但某些歌曲不能播放

歌单读取权限和单曲播放权限并不相同。会员等级、地区、歌曲下架、试听限制或登录票据过期都可能造成这种情况。Faveradio 不会伪造完整音源权限。

### AI 标签一直显示分析中或超时

- 点击“单独测试响应”检查 Base URL、模型和 Key。
- 确认模型支持 OpenAI-compatible Chat Completions。
- 检查服务额度、响应速度和并发限制。
- 更换为响应稳定的指令模型后重试。
- 批量失败时应用会暂停后台队列，避免持续消耗额度。

### 摄像头没有画面

- 确认 Windows 已允许桌面应用使用摄像头。
- 关闭正在独占摄像头的会议或直播软件。
- 从“轻量”档位重新开启。
- 摄像头与真实 Face Mesh/视线效果必须在物理设备上验证，静态页面检查不能替代硬件测试。

### 开发模式运行后立即退出

应用使用单实例锁。若已安装版本正在运行，开发实例可能立即退出。请先从托盘彻底关闭已安装版本，再执行 `npm start`。

## 本地开发

### 环境

- Windows 10/11 x64。
- 当前 Node.js LTS 与 npm。
- Git。

### 获取源码

```powershell
git clone https://github.com/WhatTheLuck/Mineradio.git
cd Mineradio
npm install
```

### 启动开发版

```powershell
npm start
```

### 运行检查

快速静态与回归检查：

```powershell
node scripts/quick-check.js
```

包含隐藏 Electron 运行时和真实入口恢复测试：

```powershell
node scripts/quick-check.js --full
```

### 构建 Windows 安装包

```powershell
npm run build:win
```

NSIS 安装包输出到 `dist/`。仅生成解包目录时可运行：

```powershell
npm run build:win:dir
```

### 重要目录

```text
desktop/                         Electron 主进程、预加载桥与本地存储
public/                          播放器界面、歌词、视觉和交互逻辑
public/js/modules/               按状态、视觉、播放、歌词和系统拆分的模块
public/vendor/source-visuals/    CyberRibbon 与 SpaceVenom 的隔离副本
cuefield/                        本地 AutoMix / 过渡分析
build/                           图标、NSIS 页面和打包钩子
tests/                           回归测试与运行时测试
scripts/                         QA、集成和服务测试脚本
docs/                            专题文档与发布说明
```

## 平台支持边界

当前正式发行目标是 Windows。Electron 的播放器、歌词和大部分 WebGL 视觉具有跨平台基础，但以下能力依赖 Windows：

- NSIS 安装与安全卸载逻辑。
- PowerShell/C# 原生辅助程序。
- DWM 桌面层和桌面图标层。
- Wallpaper Engine Scene 联动。
- Windows 专用内存管理和快捷方式处理。

因此，macOS/Linux 构建不能只改一个打包参数就宣称功能等价；需要为这些能力提供显式降级、替代实现和真实设备验证。

## 第三方平台与版权说明

Faveradio 不是网易云音乐、QQ 音乐、酷狗音乐、汽水音乐或 Wallpaper Engine 的官方客户端，也不隶属于这些平台。

第三方平台接入仅用于个人学习、本地客户端体验和用户自有账号的播放辅助。使用者应遵守相应平台的用户协议、版权规则和会员权益。项目不提供绕过付费、绕过会员、破解音质或重新分发音乐内容的能力。

CyberRibbon、SpaceVenom、第三方库和外部服务分别遵循各自的许可与服务条款。具体归属和端口说明见 [NOTICE.md](./NOTICE.md) 与 [第三方说明](./docs/THIRD_PARTY_PORTS.md)。

## 许可证

本项目采用 [GPL-3.0-only](./LICENSE) 许可证。

Faveradio 名称、界面视觉设计和项目原创视觉表达的相关权利归各自权利人所有；第三方代码、素材、模型和服务遵循其各自许可。
